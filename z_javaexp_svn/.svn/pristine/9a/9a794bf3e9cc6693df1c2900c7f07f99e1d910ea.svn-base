package javaexp.z02_homework.a16_jsr;

public class A1025 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
    /*
[1단계:확인] 1. 아래 sql문을 작성해보세요..
		SELECT * FROM emp;
		--	급여에 10% 증가시킨 금액 조회
		SELECT sal 급여, sal*1.1 "급여10% 인상"
		FROM emp;
		--	사원명과 입사일을 연결하여 조회
		SELECT ename||'('||hiredate||')' "사원-입사일"
		FROM emp;
		--	사원명과 해당 사원의 관리자번호와 연결하여 조회
		SELECT ename||'-'||nvl(mgr,0) 사원과관리자번호
		FROM emp;
		--	@@@(사원명)의 입사일은 @@@인데, 현재 급여와 보너스 합산액은 @@@이고,
		--	   이중에서 8%인 @@@이 세금입니다.(출력)
		SELECT ename||'의 입사일은 '||hiredate||'인데 현재 급여와 보너스 합산액은 '||(comm+sal)||'이고, 이중에서 8%인 '||(sal*0.08)||'이 세금입니다.' AS 자기소개
		FROM emp;
		--	관리자번호가 중복되지 않게 출력하세요
		SELECT DISTINCT mgr
		FROM emp;
[1단계:확인] 2. WHERE 조건문을 활용하여 아래 내용을 처리하세요
		--  급여가 3000이상인 사원정보 출력
		SELECT *
		FROM EMP
		WHERE sal>=3000;
		--	부서번호가 10이고 급여가 4000미만인 사원정보 출력
		SELECT *
		FROM EMP
		WHERE deptno=10 AND sal<4000;
[1단계:개념] 3. 컬럼=null과 컬럼 is null의 차이점을 sql을 통해서 설명하세요 
		컬럼=null은 자동형변환으로 인해 'null'문자열로 변경되어 값을 비교한다.
		하지만 null의 의미는 정의되지 않은 비어있는 값이기 때문에
		컬럼 is null을 이용하여 기존의 null의 이미 그대로 아무것도 정의되지 않은 비어있는 NULL을 찾아줄 수 있다.
    */
	}

}
